[project]
name = "mini-langfuse-sdk"
version = "0.1.0"
description = "Mini Langfuse Python SDK (M1)"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["mini_langfuse*"]
